apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# All resources will be deployed into the n8n namespace.
namespace: n8n

resources:
  - namespace.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  # Secret manifests are provided as templates.
  # Populate them with real values (or use a secret manager such as
  # Sealed Secrets / SOPS) and uncomment the lines below before applying.
  # - n8n-secret.yaml
  # - n8n-postgresql.yaml
